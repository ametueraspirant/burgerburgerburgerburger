<h1>masterful css work</h1>

<div class = "style-div">
	<form method = "POST">
		<input type = "text" id = "bergertext">
		<input type = "submit" value = "order up!" id = "order">
	</form>
</div>

<div class = "style-div">
	<h3>Will somebody eat these burgers??</h3>
	<ul>
	{{#each bergers}}
		{{#unless eaten}}
			{{> part eaten=false}}
		{{/unless}}
	{{/each}}
	</ul>
</div>

<div class = "style-div">
	<h3>Burgers of eaten's past</h3>
	<ul>
		{{#each bergers}}
			{{#if eaten}}
				{{> part eaten=true}}
			{{/if}}
		{{/each}}
	</ul>
</div>